const fs=require('fs');const puppeteer=require('C:/Users/Sahil Sahu/AppData/Local/npm-cache/_npx/ab5cd9f6d13a2312/node_modules/puppeteer');
(async()=>{const b=await puppeteer.launch({headless:true,args:['--no-sandbox']});try{const p=await b.newPage();p.on('console',m=>console.log(m.text().slice(0,150)));await p.goto('http://localhost:8443/');const data=await p.evaluate(async()=>{const {FaceLandmarker,FilesetResolver}=await import('https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.22-rc.20250304/vision_bundle.mjs');const files=await FilesetResolver.forVisionTasks('https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@0.10.22-rc.20250304/wasm');const model=await FaceLandmarker.createFromOptions(files,{baseOptions:{modelAssetPath:'https://storage.googleapis.com/mediapipe-models/face_landmarker/face_landmarker/float16/1/face_landmarker.task'},runningMode:'IMAGE',numFaces:1});const img=new Image();img.src='/src/Assets/myimage1.png';await img.decode();const result=model.detect(img);const face=result.faceLandmarks[0];if(!face)throw Error('No face detected');const edges=FaceLandmarker.FACE_LANDMARKS_TESSELATION;const adjacency=new Map();for(const {start,end} of edges){if(!adjacency.has(start))adjacency.set(start,new Set());if(!adjacency.has(end))adjacency.set(end,new Set());adjacency.get(start).add(end);adjacency.get(end).add(start)}const triangles=[];for(const [a,neighbors] of adjacency)for(const b of neighbors)if(b>a)for(const c of adjacency.get(b))if(c>b&&neighbors.has(c))triangles.push(a,b,c);model.close();return {landmarks:face.map(({x,y,z})=>[x,y,z]),triangles,oval:FaceLandmarker.FACE_LANDMARKS_FACE_OVAL.map(e=>e.start)}});fs.writeFileSync('src/components/faceLandmarks.json',JSON.stringify(data));console.log('SAVED',data.landmarks.length,'landmarks',data.triangles.length/3,'triangles')}finally{await b.close()}})();
