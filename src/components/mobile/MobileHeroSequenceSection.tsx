import HeroSequenceSection from "../ImageSequenceSection"
export default function MobileHeroSequenceSection(props: { indiaTime: string; onResumeClick: () => void }) {
  return <HeroSequenceSection {...props} />
}
